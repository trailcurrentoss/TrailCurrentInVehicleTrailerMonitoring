// secrets.h - ESP-NOW broadcast address
// Copy this file to secrets.h and configure your broadcast address
// NEVER commit secrets.h to version control

// Use broadcast address (0xFF) to send to all ESP-NOW peers,
// or set to a specific device MAC address
uint8_t broadcastAddress[] = {0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF};
